<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="iap_account_view_form" model="ir.ui.view">
        <field name="model">iap.account</field>
        <field name="inherit_id" ref="iap.iap_account_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//group[@name='account']" position="after">
                <group
                    string="Twilio account"
                    name="twilio"
                    invisible="provider != 'twilio'"
                >
                    <field name="twilio_production_env" widget="boolean_toggle" />
                    <field
                        name="twilio_test_account_sid"
                        invisible="twilio_production_env"
                    />
                    <field
                        name="twilio_test_auth_token"
                        password="True"
                        invisible="twilio_production_env"
                    />
                    <field
                        name="twilio_account_sid"
                        required="twilio_production_env and provider == 'twilio'"
                    />
                    <field
                        name="twilio_auth_token"
                        password="True"
                        required="twilio_production_env and provider == 'twilio'"
                    />
                    <field
                        name="twilio_number_id"
                        required="twilio_production_env and provider == 'twilio'"
                    />
                    <field name="twilio_balance_account" />
                    <button
                        name="retrieve_phone_numbers"
                        type="object"
                        string="Retrieve Twilio Numbers"
                    />
                </group>
            </xpath>
        </field>
    </record>

    <record id="sms_iap_account_view_form" model="ir.ui.view">
        <field name="name">iap.account.sms.view.form</field>
        <field name="model">iap.account</field>
        <field name="inherit_id" ref="sms.iap_account_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//div[@role='status'][1]" position="attributes">
                <attribute
                    name="invisible"
                >service_name != 'sms' or (state != 'unregistered' and state) or (provider != 'odoo')</attribute>
            </xpath>
        </field>
    </record>
</odoo>
